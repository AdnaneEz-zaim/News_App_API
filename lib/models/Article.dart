class Article{
    final String sourceName;
    final String title;

    final String url;
      String image='';
    final String publishedAt;
    final String content;
    Article(this.sourceName,this.title, this.url, this.image, this.publishedAt, this.content);
}